import { CheckerOptions, DependencyUpdated } from './types.js'

const replaceDependency = (pkgData: string, deps: DependencyUpdated[], options: CheckerOptions): string => {
  return pkgData
}

export default replaceDependency
